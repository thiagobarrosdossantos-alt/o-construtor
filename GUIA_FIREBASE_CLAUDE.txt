╔══════════════════════════════════════════════════════════════════════════════╗
║                  GUIA: ATIVAR CLAUDE NO FIREBASE                             ║
║            Como integrar Claude Code com Firebase Extensions                 ║
╚══════════════════════════════════════════════════════════════════════════════╝

Data: 06 de Dezembro de 2025
Projeto: O Construtor
Autor: Claude (Sonnet 4.5)

═══════════════════════════════════════════════════════════════════════════════
📋 ÍNDICE
═══════════════════════════════════════════════════════════════════════════════

1. Pré-requisitos
2. Opção A: Firebase Extensions (Anthropic Claude)
3. Opção B: Firebase Genkit com Claude
4. Opção C: Firebase Functions com Claude API
5. Configuração de Variáveis de Ambiente
6. Testes e Verificação
7. Troubleshooting

═══════════════════════════════════════════════════════════════════════════════
🎯 1. PRÉ-REQUISITOS
═══════════════════════════════════════════════════════════════════════════════

✅ Você precisa ter:

1. Conta Firebase ativa
   → Acesse: https://console.firebase.google.com

2. Projeto Firebase criado
   → Se não tem, crie um novo projeto no console

3. Firebase CLI instalada
   → Verifique: firebase --version
   → Se não tem, instale: npm install -g firebase-tools

4. API Key do Claude (Anthropic)
   → Você já tem: ANTHROPIC_API_KEY no .env
   → Formato: sk-ant-...

5. Node.js instalado
   → Verifique: node --version
   → Recomendado: v18 ou superior

═══════════════════════════════════════════════════════════════════════════════
🔥 2. OPÇÃO A: FIREBASE EXTENSIONS (Mais Fácil)
═══════════════════════════════════════════════════════════════════════════════

⚠️ IMPORTANTE: Ainda não existe extensão oficial Anthropic no Firebase
   Mas você pode criar uma custom extension!

──────────────────────────────────────────────────────────────────────────────
PASSO 1: Login no Firebase
──────────────────────────────────────────────────────────────────────────────

1. Abra o terminal/PowerShell
2. Execute:

   firebase login

3. Uma janela do navegador vai abrir
4. Faça login com sua conta Google
5. Autorize o Firebase CLI

──────────────────────────────────────────────────────────────────────────────
PASSO 2: Inicializar Firebase no Projeto
──────────────────────────────────────────────────────────────────────────────

1. Navegue até seu projeto:

   cd C:\Users\thiagobarrosdossanto\Desktop\o-construtor

2. Inicialize Firebase:

   firebase init

3. Selecione (use ESPAÇO para marcar):
   [ ] Firestore
   [X] Functions
   [X] Hosting (opcional)
   [ ] Storage

4. Escolha seu projeto Firebase existente ou crie novo

5. Selecione linguagem:
   → TypeScript (recomendado)

6. Instalar dependências?
   → Yes

──────────────────────────────────────────────────────────────────────────────
PASSO 3: Instalar Anthropic SDK nas Functions
──────────────────────────────────────────────────────────────────────────────

1. Entre na pasta functions:

   cd functions

2. Instale o SDK da Anthropic:

   npm install @anthropic-ai/sdk

3. Instale tipos (TypeScript):

   npm install --save-dev @types/node

──────────────────────────────────────────────────────────────────────────────
PASSO 4: Configurar API Key no Firebase
──────────────────────────────────────────────────────────────────────────────

1. Volte para a raiz do projeto:

   cd ..

2. Configure a variável de ambiente:

   firebase functions:secrets:set ANTHROPIC_API_KEY

3. Cole sua API Key quando solicitado:
   → sk-ant-...

4. Confirme a configuração

──────────────────────────────────────────────────────────────────────────────
PASSO 5: Criar Function que usa Claude
──────────────────────────────────────────────────────────────────────────────

1. Abra: functions/src/index.ts

2. Adicione este código:

   ┌──────────────────────────────────────────────────────────────────────┐
   │ import * as functions from "firebase-functions";                     │
   │ import Anthropic from "@anthropic-ai/sdk";                          │
   │                                                                      │
   │ const anthropic = new Anthropic({                                   │
   │   apiKey: process.env.ANTHROPIC_API_KEY,                           │
   │ });                                                                 │
   │                                                                      │
   │ export const askClaude = functions.https.onCall(                    │
   │   async (data, context) => {                                       │
   │     try {                                                           │
   │       const message = await anthropic.messages.create({            │
   │         model: "claude-sonnet-4-5-20250929",                       │
   │         max_tokens: 1024,                                          │
   │         messages: [{                                               │
   │           role: "user",                                            │
   │           content: data.prompt,                                    │
   │         }],                                                        │
   │       });                                                          │
   │                                                                      │
   │       return {                                                      │
   │         response: message.content[0].text,                         │
   │         model: "claude-sonnet-4-5",                                │
   │       };                                                           │
   │     } catch (error) {                                              │
   │       console.error("Error calling Claude:", error);               │
   │       throw new functions.https.HttpsError(                        │
   │         "internal",                                                │
   │         "Failed to call Claude API"                                │
   │       );                                                           │
   │     }                                                              │
   │   }                                                                │
   │ );                                                                 │
   └──────────────────────────────────────────────────────────────────────┘

3. Salve o arquivo

──────────────────────────────────────────────────────────────────────────────
PASSO 6: Deploy para Firebase
──────────────────────────────────────────────────────────────────────────────

1. Faça build das functions:

   cd functions
   npm run build

2. Volte para raiz:

   cd ..

3. Faça deploy:

   firebase deploy --only functions

4. Aguarde conclusão (pode levar 2-5 minutos)

5. Copie a URL da function que aparecerá no terminal

──────────────────────────────────────────────────────────────────────────────
PASSO 7: Testar a Function
──────────────────────────────────────────────────────────────────────────────

1. No Firebase Console:
   → Vá em "Functions"
   → Clique na function "askClaude"
   → Clique em "Test"

2. Teste com este JSON:

   {
     "prompt": "Olá! Você está funcionando no Firebase?"
   }

3. Veja a resposta do Claude!

═══════════════════════════════════════════════════════════════════════════════
⚡ 3. OPÇÃO B: FIREBASE GENKIT COM CLAUDE (Avançado)
═══════════════════════════════════════════════════════════════════════════════

Firebase Genkit é um framework para criar apps com IA.

──────────────────────────────────────────────────────────────────────────────
PASSO 1: Criar projeto Genkit
──────────────────────────────────────────────────────────────────────────────

1. Instale Genkit CLI:

   npm install -g genkit

2. Crie novo projeto:

   genkit init

3. Escolha:
   → Template: "Firebase"
   → Provider: "Custom" (vamos adicionar Claude)

──────────────────────────────────────────────────────────────────────────────
PASSO 2: Configurar Claude Provider
──────────────────────────────────────────────────────────────────────────────

1. Instale Anthropic SDK:

   npm install @anthropic-ai/sdk

2. Crie arquivo: src/claude-provider.ts

   ┌──────────────────────────────────────────────────────────────────────┐
   │ import { ModelProvider } from '@genkit-ai/core';                     │
   │ import Anthropic from '@anthropic-ai/sdk';                          │
   │                                                                      │
   │ export const claudeProvider: ModelProvider = {                      │
   │   name: 'claude',                                                   │
   │   models: {                                                         │
   │     'claude-sonnet-4-5': {                                         │
   │       generate: async (request) => {                               │
   │         const anthropic = new Anthropic({                          │
   │           apiKey: process.env.ANTHROPIC_API_KEY,                   │
   │         });                                                        │
   │                                                                      │
   │         const response = await anthropic.messages.create({         │
   │           model: 'claude-sonnet-4-5-20250929',                    │
   │           max_tokens: 1024,                                        │
   │           messages: request.messages,                              │
   │         });                                                        │
   │                                                                      │
   │         return {                                                    │
   │           text: response.content[0].text,                          │
   │           model: 'claude-sonnet-4-5',                             │
   │         };                                                         │
   │       },                                                           │
   │     },                                                             │
   │   },                                                               │
   │ };                                                                 │
   └──────────────────────────────────────────────────────────────────────┘

3. Registre provider no index.ts:

   import { claudeProvider } from './claude-provider';
   configureGenkit({ providers: [claudeProvider] });

──────────────────────────────────────────────────────────────────────────────
PASSO 3: Deploy Genkit
──────────────────────────────────────────────────────────────────────────────

1. Build:

   npm run build

2. Deploy:

   firebase deploy --only functions

═══════════════════════════════════════════════════════════════════════════════
🛠️ 4. OPÇÃO C: FIREBASE FUNCTIONS COM CLAUDE API (Simples)
═══════════════════════════════════════════════════════════════════════════════

Esta é a opção mais direta se você só quer usar Claude.

──────────────────────────────────────────────────────────────────────────────
CÓDIGO COMPLETO - functions/src/index.ts
──────────────────────────────────────────────────────────────────────────────

import * as functions from "firebase-functions";
import * as admin from "firebase-admin";
import Anthropic from "@anthropic-ai/sdk";

admin.initializeApp();

const anthropic = new Anthropic({
  apiKey: process.env.ANTHROPIC_API_KEY,
});

// ═══════════════════════════════════════════════════════════════════════
// 1. CALLABLE FUNCTION - Para chamar do app
// ═══════════════════════════════════════════════════════════════════════

export const askClaude = functions.https.onCall(async (data, context) => {
  // Verificar autenticação (opcional)
  if (!context.auth) {
    throw new functions.https.HttpsError(
      "unauthenticated",
      "User must be authenticated"
    );
  }

  const { prompt, systemPrompt, maxTokens = 1024 } = data;

  try {
    const message = await anthropic.messages.create({
      model: "claude-sonnet-4-5-20250929",
      max_tokens: maxTokens,
      system: systemPrompt || "You are a helpful assistant.",
      messages: [
        {
          role: "user",
          content: prompt,
        },
      ],
    });

    return {
      success: true,
      response: message.content[0].text,
      model: "claude-sonnet-4-5",
      usage: message.usage,
    };
  } catch (error: any) {
    console.error("Claude API Error:", error);
    throw new functions.https.HttpsError(
      "internal",
      error.message || "Failed to call Claude API"
    );
  }
});

// ═══════════════════════════════════════════════════════════════════════
// 2. HTTP FUNCTION - Para chamar via REST API
// ═══════════════════════════════════════════════════════════════════════

export const claudeAPI = functions.https.onRequest(async (req, res) => {
  // CORS
  res.set("Access-Control-Allow-Origin", "*");
  res.set("Access-Control-Allow-Methods", "POST");
  res.set("Access-Control-Allow-Headers", "Content-Type");

  if (req.method === "OPTIONS") {
    res.status(204).send("");
    return;
  }

  if (req.method !== "POST") {
    res.status(405).json({ error: "Method not allowed" });
    return;
  }

  const { prompt, systemPrompt } = req.body;

  if (!prompt) {
    res.status(400).json({ error: "Prompt is required" });
    return;
  }

  try {
    const message = await anthropic.messages.create({
      model: "claude-sonnet-4-5-20250929",
      max_tokens: 1024,
      system: systemPrompt || "You are a helpful assistant.",
      messages: [{ role: "user", content: prompt }],
    });

    res.json({
      success: true,
      response: message.content[0].text,
      model: "claude-sonnet-4-5",
    });
  } catch (error: any) {
    console.error("Error:", error);
    res.status(500).json({ error: error.message });
  }
});

// ═══════════════════════════════════════════════════════════════════════
// 3. FIRESTORE TRIGGER - Responde a mudanças no Firestore
// ═══════════════════════════════════════════════════════════════════════

export const processMessageWithClaude = functions.firestore
  .document("messages/{messageId}")
  .onCreate(async (snap, context) => {
    const messageData = snap.data();

    if (!messageData.prompt) return;

    try {
      const response = await anthropic.messages.create({
        model: "claude-sonnet-4-5-20250929",
        max_tokens: 1024,
        messages: [{ role: "user", content: messageData.prompt }],
      });

      // Salvar resposta no Firestore
      await snap.ref.update({
        response: response.content[0].text,
        processedAt: admin.firestore.FieldValue.serverTimestamp(),
        model: "claude-sonnet-4-5",
      });

      console.log("Message processed:", context.params.messageId);
    } catch (error) {
      console.error("Error processing message:", error);
      await snap.ref.update({
        error: error.message,
        processedAt: admin.firestore.FieldValue.serverTimestamp(),
      });
    }
  });

═══════════════════════════════════════════════════════════════════════════════
🔐 5. CONFIGURAÇÃO DE VARIÁVEIS DE AMBIENTE
═══════════════════════════════════════════════════════════════════════════════

──────────────────────────────────────────────────────────────────────────────
MÉTODO 1: Firebase Secrets (Recomendado)
──────────────────────────────────────────────────────────────────────────────

1. Configure o secret:

   firebase functions:secrets:set ANTHROPIC_API_KEY

2. Cole sua API key: sk-ant-...

3. No código, acesse com: process.env.ANTHROPIC_API_KEY

4. Deploy:

   firebase deploy --only functions

──────────────────────────────────────────────────────────────────────────────
MÉTODO 2: Firebase Config (Menos Seguro)
──────────────────────────────────────────────────────────────────────────────

1. Configure:

   firebase functions:config:set anthropic.key="sk-ant-..."

2. No código:

   const apiKey = functions.config().anthropic.key;

3. Deploy

──────────────────────────────────────────────────────────────────────────────
MÉTODO 3: .env Local (Apenas para testes)
──────────────────────────────────────────────────────────────────────────────

1. Crie: functions/.env

   ANTHROPIC_API_KEY=sk-ant-...

2. Instale dotenv:

   npm install dotenv

3. No código:

   import 'dotenv/config';

⚠️ NUNCA faça commit do .env! Adicione ao .gitignore

═══════════════════════════════════════════════════════════════════════════════
✅ 6. TESTES E VERIFICAÇÃO
═══════════════════════════════════════════════════════════════════════════════

──────────────────────────────────────────────────────────────────────────────
Testar Localmente
──────────────────────────────────────────────────────────────────────────────

1. Inicie emulador:

   firebase emulators:start

2. Acesse: http://localhost:4000

3. Vá em "Functions"

4. Teste suas functions

──────────────────────────────────────────────────────────────────────────────
Testar em Produção (Callable Function)
──────────────────────────────────────────────────────────────────────────────

1. Do seu app Python, use requests:

   ┌──────────────────────────────────────────────────────────────────────┐
   │ import requests                                                      │
   │                                                                      │
   │ url = "https://us-central1-SEU-PROJETO.cloudfunctions.net/askClaude"│
   │                                                                      │
   │ data = {                                                            │
   │     "data": {                                                       │
   │         "prompt": "Explique o que é Firebase em 3 linhas"          │
   │     }                                                               │
   │ }                                                                   │
   │                                                                      │
   │ response = requests.post(url, json=data)                            │
   │ print(response.json())                                              │
   └──────────────────────────────────────────────────────────────────────┘

──────────────────────────────────────────────────────────────────────────────
Testar HTTP Function
──────────────────────────────────────────────────────────────────────────────

1. Com curl:

   curl -X POST https://us-central1-SEU-PROJETO.cloudfunctions.net/claudeAPI \
     -H "Content-Type: application/json" \
     -d '{"prompt": "Olá Claude no Firebase!"}'

2. Com Python:

   import requests

   response = requests.post(
       "https://us-central1-SEU-PROJETO.cloudfunctions.net/claudeAPI",
       json={"prompt": "Olá Claude!"}
   )
   print(response.json())

──────────────────────────────────────────────────────────────────────────────
Ver Logs
──────────────────────────────────────────────────────────────────────────────

1. No terminal:

   firebase functions:log

2. No Firebase Console:
   → Functions → Logs

═══════════════════════════════════════════════════════════════════════════════
🔧 7. TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════════

──────────────────────────────────────────────────────────────────────────────
Erro: "ANTHROPIC_API_KEY não configurada"
──────────────────────────────────────────────────────────────────────────────

✅ Solução:
   firebase functions:secrets:set ANTHROPIC_API_KEY
   firebase deploy --only functions

──────────────────────────────────────────────────────────────────────────────
Erro: "Module @anthropic-ai/sdk not found"
──────────────────────────────────────────────────────────────────────────────

✅ Solução:
   cd functions
   npm install @anthropic-ai/sdk
   cd ..
   firebase deploy --only functions

──────────────────────────────────────────────────────────────────────────────
Erro: "Insufficient permissions"
──────────────────────────────────────────────────────────────────────────────

✅ Solução:
   1. Vá em Firebase Console → Project Settings
   2. Service Accounts → Permissions
   3. Adicione role "Cloud Functions Developer"

──────────────────────────────────────────────────────────────────────────────
Erro: "Function deployment timeout"
──────────────────────────────────────────────────────────────────────────────

✅ Solução:
   1. Aumente timeout no código:

      export const askClaude = functions
        .runWith({ timeoutSeconds: 540 })
        .https.onCall(...)

   2. Ou no firebase.json:

      "functions": {
        "source": "functions",
        "runtime": "nodejs18",
        "timeout": "540s"
      }

──────────────────────────────────────────────────────────────────────────────
Erro: "CORS error"
──────────────────────────────────────────────────────────────────────────────

✅ Solução: Adicione headers CORS:

   res.set('Access-Control-Allow-Origin', '*');
   res.set('Access-Control-Allow-Methods', 'GET, POST');

──────────────────────────────────────────────────────────────────────────────
Function muito lenta
──────────────────────────────────────────────────────────────────────────────

✅ Solução:
   1. Aumente memória:

      functions.runWith({ memory: '1GB' })

   2. Use region mais próxima:

      functions.region('southamerica-east1')

═══════════════════════════════════════════════════════════════════════════════
📚 RECURSOS ADICIONAIS
═══════════════════════════════════════════════════════════════════════════════

Documentação Oficial:
→ Firebase Functions: https://firebase.google.com/docs/functions
→ Anthropic API: https://docs.anthropic.com
→ Firebase Secrets: https://firebase.google.com/docs/functions/config-env

Exemplos:
→ GitHub Firebase Samples: https://github.com/firebase/functions-samples
→ Anthropic SDK Examples: https://github.com/anthropics/anthropic-sdk-typescript

Comunidade:
→ Firebase Discord: https://discord.gg/firebase
→ Stack Overflow: [firebase-cloud-functions]

═══════════════════════════════════════════════════════════════════════════════
✨ PRÓXIMOS PASSOS
═══════════════════════════════════════════════════════════════════════════════

Depois de configurar Claude no Firebase, você pode:

1. ✅ Integrar com O Construtor
   → Substituir chamadas diretas por Firebase Functions
   → Usar Firebase como cache de respostas

2. ✅ Adicionar Firestore
   → Salvar histórico de conversas
   → Armazenar resultados de debates

3. ✅ Criar Dashboard
   → Monitorar uso de API
   → Ver custos em tempo real

4. ✅ Implementar Rate Limiting
   → Prevenir abuso
   → Controlar custos

5. ✅ Adicionar Authentication
   → Firebase Auth
   → Proteger suas functions

═══════════════════════════════════════════════════════════════════════════════
📞 SUPORTE
═══════════════════════════════════════════════════════════════════════════════

Se tiver problemas:

1. Verifique os logs: firebase functions:log
2. Teste localmente: firebase emulators:start
3. Verifique billing: Firebase Console → Usage and Billing
4. Consulte a documentação oficial

Bom desenvolvimento! 🚀

═══════════════════════════════════════════════════════════════════════════════
FIM DO GUIA
═══════════════════════════════════════════════════════════════════════════════
